---
wave: 1
depends_on: []
files_modified: [
  "backend/database.py",
  "backend/models.py",
  "backend/deps.py",
  "backend/main.py"
]
requirements: ["Project/Simulation/Agent models.", "SQLModel tables created in SQLite."]
autonomous: true
---

# Plan: 02-01 Database & Modeling

Establish the persistent data layer using SQLModel and SQLite with async support and WAL mode.

## User Review Required

> [!IMPORTANT]
> **Data Model Updates**:
> I have included `age_group` and `income` in the `Agent` model as requested.
> We're using `aiosqlite` which requires `sqlite+aiosqlite://` in the connection string.

## Tasks

### 1. Database Connection & WAL Mode
Set up the asynchronous SQLAlchemy engine and enable SQLite's WAL mode for concurrency.

<task>
<read_first>
- 02-RESEARCH.md
- backend/config.py
</read_first>
<action>
1. Create `backend/database.py`.
2. Define `AsyncSession` and `create_async_engine` using settings from `config.py`.
3. Add a listener to `engine.sync_engine` for the "connect" event to execute `PRAGMA journal_mode=WAL`.
</action>
<acceptance_criteria>
- backend/database.py exists
- WAL pragma is correctly set via listener
</acceptance_criteria>
</task>

### 2. Core Data Models
Implement the SQLModel entities for the simulation.

<task>
<read_first>
- policysim-prompt.md
</read_first>
<action>
1. Create `backend/models.py`.
2. Define `Simulation` (title, project_context, current_round, status).
3. Define `Agent` (archetype, persona, age_group, income, initial_stance, influence, regional_data).
4. Define `Round` (round_number, event_text, synthesis_text).
5. Define `AgentRoundResult` (stance_value, sentiment, round_id, agent_id).
6. Set up relationships (one-to-many from Simulation → Round, Simulation → Agent, etc.).
</action>
<acceptance_criteria>
- backend/models.py contains all specified classes
- Agent model includes age_group and income fields
- Relationships are correctly defined for async loads
</acceptance_criteria>
</task>

### 3. Session Dependency
Add FastAPI dependency injection for database sessions.

<task>
<read_first>
- backend/database.py
</read_first>
<action>
1. Create `backend/deps.py`.
2. Implement `get_session` async generator that yields an `AsyncSession`.
</action>
<acceptance_criteria>
- backend/deps.py provides a functional get_session dependency
</acceptance_criteria>
</task>

### 4. App Lifespan & Table Creation
Register the database initialization in the FastAPI lifespan.

<task>
<read_first>
- backend/main.py
- backend/database.py
</read_first>
<action>
1. Update `backend/main.py`.
2. Import `lifespan` from `contextlib`.
3. Define the async `lifespan` function to run `SQLModel.metadata.create_all` (using `run_sync` on the async connection).
4. Register the lifespan in the `FastAPI` app constructor.
</action>
<acceptance_criteria>
- backend/main.py includes lifespan logic
- Tables are created on app startup
</acceptance_criteria>
</task>

## Verification

### must_haves
- [ ] Database file `policysim.db` is created on first run.
- [ ] `sqlite3 policysim.db "PRAGMA journal_mode"` returns `wal`.
- [ ] All tables specified in `models.py` exist in the database.
- [ ] `Agent` table has `age_group` and `income` columns.

### Manual Verification
- Run a small script `scripts/verify_db.py` that inserts a Simulation + Agent and verifies successful retrieval with all fields.
