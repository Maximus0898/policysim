---
wave: 2
depends_on: ["08-01-PLAN.md"]
files_modified: [
  "frontend/src/routes/+page.svelte",
  "frontend/src/lib/components/"
]
requirements: ["UI-04"]
autonomous: true
---

# Plan: 08-02 Dashboard Interactive Elements

Build the `WorldBuilder` control panel and the live simulation tracking view.

## User Review Required

> [!IMPORTANT]
> **View Structure**:
> - We will structure the root page (`/+page.svelte`) as a single-page interactive command center. 
> - **Left Panel**: Form to draft simulations (`POST /api/simulations/`).
> - **Right Panel**: Streams the results natively when a run is initiated via `EventSource`. The state will be modeled via Svelte 5 `$state()` rules.

## Tasks

### 1. HTTP Configuration
Set up global API references for Svelte requests.

<task>
<read_first>
- 07-RESEARCH.md
</read_first>
<action>
1. Set up `frontend/.env.local` to define `PUBLIC_API_URL=http://localhost:8000`.
</action>
<acceptance_criteria>
- Frontend properly queries the FastAPI service via env variables.
</acceptance_criteria>
</task>

### 2. Live Dashboard Implementation
Implement the view logic.

<task>
<read_first>
- frontend/src/routes/+page.svelte
</read_first>
<action>
1. Create `frontend/src/lib/components/DraftForm.svelte` to post data to `DraftSimulationRequest` and store the `simulation_id`.
2. Create `frontend/src/lib/components/StreamView.svelte` that utilizes standard JS `EventSource` to attach to `/api/simulations/{id}/stream`.
3. Implement Runes to track round arrays dynamically.
4. Render incoming updates sequentially with a sleek micro-animation scale-in.
</action>
<acceptance_criteria>
- A user can trigger a simulation fully end-to-end via the UI.
</acceptance_criteria>
</task>

## Verification

### must_haves
- [ ] Forms validate correctly.
- [ ] `EventSource` handles incoming JSON payload chunks from FastAPI.

### Manual Verification
- We will boot FastAPI and SvelteKit simultaneously, draft a mock "Policy 1" from the browser, and watch the event logs trickle down the page.
