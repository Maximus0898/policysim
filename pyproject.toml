[project]
name = "policysim"
version = "0.1.0"
description = "Multi-agent policy simulation"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "httpx>=0.28.1",
    "pydantic>=2.12.5",
    "sse-starlette>=3.3.4",
]

[tool.uv.workspace]
members = ["backend"]
